2025-12-29
在anaconda中搭建开发环境
python = 3.13
pytorch = 13.0（安装命令，见官网教程）
CUDA = 13.1（安装命令，见官网教程）

在新创建的CUDA环境中安装jupyter note
conda install nb_conda_kernels

dataset练习
python中用__init__(self)函数完成class的初始化
__getitem(self, idx)函数只有在程序引用到具体实例时才会被调用

导入dataset：from torch.utils.data import Dataset
导入图片操作： from PIL import Image
导入文件相关： import os

图片dataset的组织方式
1. 只存放图片，用图片的文件名作为数据的label存放图片，用图片的文件名作为数据的label
2. 图片和label分别存放在不同的文件中，label以同名的txt文件存放

疑问：
1. 目前还没有numpy和pandas相关调用，在conda中似乎没有安装相关的lib
2. 需要学习opencv，这是更加主流的图片处理lib

2025-12-30
tensorboard的使用
tensorboard是一种数据可视化的（debug）手段
使用tensorboard
导入 from torch.utils.tensorboard import SummaryWriter
创建实例 writer = SummaryWriter("logs") --> 会在项目目录中生成一个logs的文件夹
使用write.add_xxx()方法导入数据

导入图片过程
from PIL import Image
import numpy as np
img = Image.open("img_path")
img_arr = np.array(img)
write.add_image(tag, img_arr, step, dataformats) #dataformats需要根据img_arr.shape()修改

2026-01-03
Transforms的使用
1. 创建transforms实例
2. 通过transforms实例的方法直接导入图片
3. 可以将tensor类型的图片直接导入torch的tensorboard

2026-01-04，2026-01-05
useful transform method
ToTensor
Normalize
Resize
Compose
RandomCrop

2026-01-20
1. 下载，训练，使用pytorch标准数据集
2. 手动下载pytorch数据集以及如何查找数据集的下载地址

2026-01-20
使用dataloader从dataset中抽取样本

类
class torch.utils.data.DataLoader(dataset, batch_size=1, shuffle=None, sampler=None, batch_sampler=None, num_workers=0, collate_fn=None, pin_memory=False, drop_last=False, timeout=0, worker_init_fn=None, multiprocessing_context=None, generator=None, *, prefetch_factor=None, persistent_workers=False, pin_memory_device='', in_order=True)

关键参数
batch_size (int, optional) – how many samples per batch to load (default: 1). # 每次抽取样本的数量
shuffle (bool, optional) – set to True to have the data reshuffled at every epoch (default: False). # 每次抽取之前是否乱序
num_workers (int, optional) – how many subprocesses to use for data loading. 0 means that the data will be loaded in the main process. (default: 0) # 是否使用多进程（windows下可能会报错）
drop_last (bool, optional) – set to True to drop the last incomplete batch, if the dataset size is not divisible by the batch size. If False and the size of dataset is not divisible by the batch size, then the last batch will be smaller. (default: False) # 如果最后剩余的样本数少于batch_size，是否舍去

在fstring中，"{{}}"转义为"{}"
tensorboard中，可以在log文件夹中删去相应的输出文件，重新运行tensorboard命令来移除网页中错误的tag

2026-01-21
学习torch.nn.Containers

class Model(nn.Module): # class Model 继承于父类 nn.module
    def __init__(self) -> None:
        super().__init__() # 调用父类的初始化函数（必须步骤）
        self.conv1 = nn.Conv2d(1, 20, 5)
        self.conv2 = nn.Conv2d(20, 20, 5)

    def forward(self, x):
		# forward中 
        x = F.relu(self.conv1(x)) # x先经过一次卷积self.covl(x)，再经过一次非线性处理F.relu()
        return F.relu(self.conv2(x)) # 在经过一次卷积self.conv2(x)，在经过一次非线性处理F.relu()





